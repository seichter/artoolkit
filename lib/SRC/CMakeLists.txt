# Core library
ADD_SUBDIRECTORY(AR)
ADD_SUBDIRECTORY(ARMulti)

# GL utility
ADD_SUBDIRECTORY(Gl)

# Video
IF   (APPLE)
	ADD_SUBDIRECTORY(VideoMacOSX)
ENDIF(APPLE)

IF   (UNIX)
	IF   (USE_GSTREAMER)
		ADD_SUBDIRECTORY(VideoGStreamer)
	ENDIF(USE_GSTREAMER)
ENDIF(UNIX)
